---
/**
 * TopNav — full-width strip used on Home, Chapter Index, About, Logs.
 *
 * Layout: lockup left, nav links right. Border-bottom 1px rule.
 * On <=720px, links collapse to a hamburger placeholder (CSS-only details/summary).
 *
 * Five links. The lockup is the way home. "The Book" goes to the chapter
 * index and stays lit on Home, the index, and every reading page. Source
 * and Download live in the site footer.
 */
import Lockup from './Lockup.astro';

interface Props {
  active?: 'book' | 'chapters' | 'loadout' | 'rack' | 'logs' | 'about' | 'source' | undefined;
}
const { active: rawActive } = Astro.props;
const active = rawActive === 'chapters' ? 'book' : rawActive;

const links = [
  { key: 'book',     label: 'The Book',     href: '/chapters' },
  { key: 'loadout',  label: 'The Loadout',  href: '/loadout' },
  { key: 'rack',     label: 'The Rack',     href: '/rack' },
  { key: 'logs',     label: 'Logs',         href: '/logs' },
  { key: 'about',    label: 'About',        href: '/about' },
] as const;
---
<header class="top-nav">
  <a href="/" class="brand" aria-label="Mission Built, home">
    <Lockup size={16} bare />
  </a>

  <nav class="nav-links" aria-label="Primary">
    <ul>
      {links.map((l) => (
        <li>
          <a
            href={l.href}
            class:list={["nav-link", { active: active === l.key }]}
            aria-current={active === l.key ? 'page' : undefined}
          >
            {l.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>

  <details class="mobile-menu">
    <summary aria-label="Menu">
      <span class="bars" aria-hidden="true">
        <span></span><span></span><span></span>
      </span>
    </summary>
    <ul class="mobile-list">
      {links.map((l) => (
        <li>
          <a
            href={l.href}
            class:list={["nav-link", { active: active === l.key }]}
            aria-current={active === l.key ? 'page' : undefined}
          >
            {l.label}
          </a>
        </li>
      ))}
    </ul>
  </details>
</header>

<style>
  .top-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 28px;
    border-bottom: var(--border-thin);
    background: var(--color-bg);
    position: relative;
    z-index: 5;
  }

  .brand { display: inline-flex; align-items: center; }

  .nav-links ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    gap: 28px;
  }

  .nav-link {
    font-family: var(--font-mono);
    font-weight: 400;
    font-size: 12px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-chalk-dim);
    transition: color 120ms ease-out;
    white-space: nowrap;
  }
  .nav-link:hover { color: var(--color-chalk); }
  /* Active: chalk plus a small oxblood square, so the state isn't carried
     by a brightness shift alone. */
  .nav-link.active {
    color: var(--color-chalk);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .nav-link.active::before {
    content: '';
    width: 6px;
    height: 6px;
    background: var(--color-blood);
    flex-shrink: 0;
  }

  /* ------- Mobile menu ------- */
  .mobile-menu { display: none; }
  .mobile-menu summary {
    list-style: none;
    cursor: pointer;
    padding: 6px 4px;
  }
  .mobile-menu summary::-webkit-details-marker { display: none; }

  .bars {
    display: inline-flex;
    flex-direction: column;
    gap: 4px;
    width: 22px;
  }
  .bars > span {
    display: block;
    width: 100%;
    height: 1px;
    background: var(--color-chalk);
  }

  .mobile-list {
    list-style: none;
    margin: 0;
    padding: 14px 0 4px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  /* ------- Responsive ------- */
  @media (max-width: 1000px) {
    .nav-links ul { gap: 18px; }
  }
  @media (max-width: 720px) {
    .top-nav {
      padding: 14px 24px;
      flex-wrap: wrap;
      align-items: center;
    }
    .nav-links { display: none; }
    .mobile-menu { display: block; }
    .mobile-menu[open] {
      width: 100%;
      order: 3;
      border-top: var(--border-thin);
      margin-top: 14px;
      padding-top: 4px;
    }
  }
</style>
