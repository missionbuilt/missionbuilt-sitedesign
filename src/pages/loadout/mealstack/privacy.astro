---
/**
 * TEMPORARY. MealStack 0.8.0 is in App Review with the privacy URL
 * https://missionbuilt.io/loadout/mealstack/privacy in App Store Connect and
 * in the build. This shim serves the Rack privacy page at the old address,
 * byte for byte, so nothing the reviewer opens changes mid-review.
 *
 * After approval: delete this file, and in public/_redirects replace the two
 * explicit /loadout/mealstack rules with the splat rule
 *   /loadout/mealstack/*   /rack/mealstack/:splat  301
 * so the old privacy URL becomes a 301 like everything else (installed builds
 * link it, so the redirect is permanent).
 */
import Privacy from '../../rack/mealstack/privacy.astro';
---
<Privacy />
