// This file is no longer needed since training logs have been removed
// Keeping as empty placeholder to avoid import errors during cleanup

export const loadLogContent = async (logId: number): Promise<null> => {
  return null;
};
